# Auth Service

Auth service skeleton. Implementation will be added later.

## Environment

Planned variables (subject to change):

- `NODE_ENV`
- `PORT`
- `AUTH_DATABASE_URL`
- `GOOGLE_CLIENT_ID` (optional)
- `GOOGLE_CLIENT_SECRET` (optional)

{
  "name": "@template/sandbox-frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Disposable frontend sandbox for auth and API flow testing",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file .env --env-file-if-exists .env.local ./server.mjs",
    "start": "node --env-file .env --env-file-if-exists .env.local ./server.mjs"
  }
}
